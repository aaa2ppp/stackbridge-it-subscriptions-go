apiVersion: v1
kind: Service
metadata:
  name: server
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: ${SERVER_NODE_PORT}   # внешний порт
  selector:
    app: server
